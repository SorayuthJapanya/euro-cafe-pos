FROM node:22-alpine

RUN npm install -g pnpm

WORKDIR /app

EXPOSE 5173

# pnpm install runs at container start so node_modules volume is populated
CMD ["sh", "-c", "pnpm install && pnpm dev --host 0.0.0.0"]
