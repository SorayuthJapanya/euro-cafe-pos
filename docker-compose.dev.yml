services:

  # ─── MySQL 8.0 ────────────────────────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: eurocafe_mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  # ─── Laravel API ───────────────────────────────────────────────
  # server:
  #   build:
  #     context: .docker/containers/server
  #     dockerfile: Dockerfile.dev
  #   container_name: eurocafe_server
  #   ports:
  #     - "${SERVER_PORT:-8000}:8000"
  #   volumes:
  #     - ./server:/var/www/html          # live code reload
  #     - vendor_cache:/var/www/html/vendor   # keep vendor inside container
  #   environment:
  #     APP_NAME: ${APP_NAME:-Euro Cafe POS}
  #     APP_ENV: local
  #     APP_DEBUG: ${APP_DEBUG:-true}
  #     APP_URL: http://localhost:${SERVER_PORT:-8000}
  #     DB_CONNECTION: mysql
  #     DB_HOST: mysql
  #     DB_PORT: "3306"
  #     DB_DATABASE: ${MYSQL_DATABASE}
  #     DB_USERNAME: root
  #     DB_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     SESSION_DRIVER: database
  #     SESSION_LIFETIME: "120"
  #     CACHE_STORE: database
  #     QUEUE_CONNECTION: database
  #     LOG_CHANNEL: stack
  #     LOG_LEVEL: debug
  #     BCRYPT_ROUNDS: "12"
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   restart: unless-stopped

  # # ─── Vue 3 + Vite client ──────────────────────────────────────
  # client:
  #   build:
  #     context: .docker/containers/client
  #     dockerfile: Dockerfile.dev
  #   container_name: eurocafe_client
  #   ports:
  #     - "${CLIENT_PORT:-8090}:5173"
  #   volumes:
  #     - ./client:/app                   # live code reload
  #     - node_modules_cache:/app/node_modules
  #   depends_on:
  #     - server
  #   restart: unless-stopped

volumes:
  mysql_data:
  vendor_cache:
  node_modules_cache:
